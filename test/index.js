/**
 * TODO: test
 */ 
// import fetch from 'isomorphic-unfetch';
// import { wrapFetch } from '../src/wrap-fetch';
import { expect } from 'chai';

describe('wrapFetch', () => {
  it('works', () => {
    expect(false).to.equal(false);
  });
});
